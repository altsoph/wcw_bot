import logging

class proto_annotator(object):
	main_cfg = None
	def __init__(self,cfg):
		self.main_cfg = cfg
	def get(self,img,items,parameters = None):
		""" 
        gets img, items, pars
        returns text annotation 
   		"""
		pass
