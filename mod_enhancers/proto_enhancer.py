import logging

class proto_enhancer(object):
	main_cfg = None
	def __init__(self,cfg):
		self.main_cfg = cfg
	def get(self, pil_img, items, parameters=None):
		""" 
		returns a decorated image or None on any error
		"""
		pass
